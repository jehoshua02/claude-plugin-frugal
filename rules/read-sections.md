MANDATORY: To find something in a file, Grep first, then Read with offset and limit around matches.
Do NOT call Read without offset and limit — unless the task requires the whole file (summarization, legal review, "what does this class do?").
