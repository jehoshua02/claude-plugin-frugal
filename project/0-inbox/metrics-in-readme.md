# Metrics in README for Every Rule

## Abstract

Add tested metrics (cost reduction, token savings) to the README for every rule, not just the ones with existing test data. Requires running tests for rules that don't have comparison data yet.
